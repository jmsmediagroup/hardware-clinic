# Distributed via GitHub Releases, not committed (keeps the repo lean)
*.img
*.iso
*.zip
*.exe

# NEVER commit — signing keys, per-stick config, signed outputs, secrets
KEY.BIN
PUBKEY.HEX
keys/
*.key
*.pem
*.SIG
CLINIC.CFG
.env
.env.*

# OS cruft
.DS_Store

# Release assets live on the Releases page, not in the source listing
/BOOTX64.EFI
/SHA256SUMS
